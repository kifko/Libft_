Command to install libft in the System
	sudo make install
Command to uninstall
	sudo make uninstall
Command to compile with libft
	gcc -I./inc main.c -L. -lft
Command in case libft is installed in the system
	gcc main-c -lft
